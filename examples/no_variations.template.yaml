# If we do not want to generate any variations for this task,
# we can simply leave out all the fields we don't care about
# and mark all the list items as `pass`.

# The !ArenaConfig and !Arena mappings must always be specified.
!ArenaConfig
arenas:
  0: !Arena
    # These fields must be commented out or removed, as we do not want to vary them.
    # pass_mark: 0
    # t: 250
    items:
    # We do not want to vary the first item, which was the Agent, at all. We simply mark it as `pass`.
    # The comment added after is purely to help ourselves remember what this item represented.
    # It is not required for the script to work.
    - pass # Agent
    # Similar for the GoodGoal
    - pass # GoodGoal